# SABO Cinematic UI — Agent Contract

## Scope

This repo owns the cinematic UI implementation and visual assets. Keep changes focused, inspect real rendered output and preserve unrelated WIP.

## GitHub routing

Canonical owner is `longsangsabo2026-max`; push to target `origin`. Legacy `source` is read-only recovery. Use GitHub CLI/keyring auth; never store credentials in `.env`, source, logs or commits.
