# CLAUDE.md — SABO Cinematic UI

> Read `AGENTS.md` first; it owns stable safety/workflow policy.

## GitHub routing

Canonical owner is `longsangsabo2026-max`; push to target `origin`. Legacy `source` is read-only recovery. Auth uses the GitHub CLI/keyring; never store credentials in `.env`, source, logs or commits.

## Verification

Use the repo's package scripts and inspect the actual rendered frame/route before reporting visual readiness.
